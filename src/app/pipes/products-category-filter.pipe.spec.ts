import { ProductsCategoryFilterPipe } from './products-category-filter.pipe';

describe('ProductsCategoryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductsCategoryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
